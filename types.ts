/**
 * A timestamp is the time elapsed since the Unix epoch in milliseconds.
 */
export type Timestamp = number; //milliseconds

/**
 * A duration is the elapsed time between two events in milliseconds.
 */
export type Duration = number; // milliseconds
